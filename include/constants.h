#pragma once

#include "utils.h"

const int FPS = 60;

const int NORMAL_MOB_SPEED = 5;

const Position PACMAN_SPAWN_POSITION = {Cell(18, 5), Direction::LEFT};
const Position GHOST_SPAWN_POSITION = {Cell(1, 10), Direction::RIGHT};
